# qt_base

> It's base on `qt5.9 base` lib.

> A simple demo using `QWidgets`.

### build

```sh
mkdir build && cd build
cmake ..
make
```

### run

> **output: `./build/bin`**

```sh
./bin/hello
```
